# Аудит Marine Track — 2026-07-10

## 1. Объём и исходное состояние

Аудит выполнен по `main` на коммите `7e7d6b0` (`Document experimental wake speed in README`). На исходном срезе в репозитории было 287 коммитов и не было открытых issues/PR; результаты аудита опубликованы в [draft PR #1](https://github.com/f2re/marine-track/pull/1). Проверены исходный код, конфигурация, документация, история последних изменений, тесты, CI-контракт, модель угроз и фактическая доступность внешних данных.

Важно разделять три уровня готовности:

1. **Инженерно реализовано** — код существует и покрыт локальными тестами.
2. **Экспериментально доступно** — эвристика выполняется, но не имеет калиброванной точности и не должна трактоваться как измерение.
3. **Научно подтверждено** — есть отложенная выборка, независимая разметка/AIS, метрики по стратифицированным условиям и оценка неопределённости.

Текущее состояние проекта в основном находится между уровнями 1 и 2. Уровень 3 ещё не достигнут.

## 2. Итоговый вывод

Marine Track уже является рабочим каркасом эксплуатационного конвейера:

```text
AOI/время → поиск STAC-сцен → выбор raster asset → AOI crop → land mask → локальный CFAR → кандидаты → Hough wake → AIS enrichment → GeoJSON/CSV/Parquet/PNG/report
```

Но это пока **не подтверждённый детектор судов и не измеритель скорости**. Корректное описание текущего результата:

> Геопривязанные яркие кандидаты на одном GeoTIFF/COG с эвристическим признаком линейного следа, внешним AIS-сопоставлением и экспериментальным proxy скорости.

Главный риск — документация и конфигурация местами описывают целевую систему как уже реализованную. До научной валидации нельзя выводить `confidence` как вероятность обнаружения судна, а `speed_knots` по wake wavelength — как надёжную скорость судна.

## 3. Что реально работает сейчас

| Область | Статус | Фактическая реализация |
|---|---|---|
| Telegram UX | реализовано | Меню, сохранённые AOI/bbox, прогресс, режим выдачи |
| Поиск сцен | частично реализовано | STAC/provider fallback, отдельный detection-capable поиск |
| Materialization | ограниченно реализовано | Только raster GeoTIFF/COG; preview и ASF ZIP/GRD не обрабатываются |
| Sentinel-1 | эвристический MVP | Один выбранный band, без полноценной калибровки/Lee-фильтра и без dual-pol fusion |
| Sentinel-2 | задел, не detection stack | Нет B02/B03/B04/B08 stack, SCL/cloud/water mask и корректного optical pipeline |
| Кандидаты судов | реализовано как candidates | Bright-target local-CFAR, connected components, площадь/форма/контраст |
| Wake | экспериментально | Canny + Hough, линия около кандидата; нет строгой V/Kelvin-геометрии |
| Курс | экспериментально | Ось линии с неустранённой 180° неоднозначностью |
| Скорость | только proxy | AIS SOG либо экспериментальная оценка по профилю пиков |
| AIS | частично реализовано | Локальный CSV, интерполяция позиции, расстояние и overlay; нет gap/QC и полноценного online provider |
| Физическая валидация | не интегрирована | `validation.py` содержит функции, но pipeline их не вызывает |
| Ocean context | не интегрирован | Copernicus Marine wrapper есть, но течение/ветер/волны не попадают в detection result |
| HTML-отчёт | не реализовано | Фактический артефакт — `report.json`, не HTML |

### 3.1. Проверяемая карта ключевых рисков

| Риск | Evidence в коде | Возможное последствие | Exit condition |
|---|---|---|---|
| Cache time/capability collision | `cache_policy.py::search_cache_key`, `pipeline.py`, `detection_scene_search.py` | Анализ сцены из другого интервала или search-only результата | Full query key + revalidation regression tests |
| Fail-open Telegram access | `telegram_bot.py::is_authorized`, `telegram_config.py` | Любой пользователь запускает дорогие operational commands | Empty allowlist denies; explicit public-mode test |
| Cross-user token replay | `telegram_scene_browser.py`, `telegram_detection.py` | Чужой AOI/scene повторно обрабатывается по token | Owner-bound registry/callback/output tests |
| Speed/reference conflation | `detection_pipeline.py`, `models.py`, `rendering/` | Kelvin proxy/AIS SOG выглядят как измеренная скорость | Separate schema + UI snapshot tests |
| Lossy asset model | `data_sources/stac_provider.py`, `scene_materializer.py` | Неверный band/auth/units; CDSE COG не читается | Typed asset + sign/range-read contract |
| Config drift | `config.py`, `config/processing.yaml`, Telegram detector call | YAML не меняет реальный runtime | Effective-config parity tests CLI/Telegram |
| AOI-specific global mask | `deploy_telegram_bot.sh::prepare_land_mask_once`, `land_mask_update.py` | Вне default AOI суша не подавляется | Global/versioned mask + arbitrary-AOI tests |
| Non-atomic live deploy/state | `deploy_telegram_bot.sh`, registry/user-state modules | Частичный релиз, race или повреждение JSON | Read-only versioned release + atomic locked writes |

## 4. Критические замечания

### P0-1. Release gate сейчас красный

В изолированном окружении Python 3.12 с установкой `.[dev]` получено:

```text
77 passed, 4 failed
```

Падают:

- `tests/test_detector_metrics.py` — на синтетическом объекте `contrast_sigma == 0`, потому что фон имеет нулевое стандартное отклонение; текущая формула не определяет fallback для детерминированного фона.
- `tests/test_runtime_check_profiles.py::test_provider_profile_alias` — тест ожидает удалённый alias `none`.
- `tests/test_runtime_check_profiles.py::test_numeric_env_validation_catches_invalid_values` — тест ожидает старое сообщение `must be integer`, код пишет `must be numeric`.
- `tests/test_smoke_check.py` — тест вызывает устаревший аргумент `check_telegram`.

`ruff check src tests` также не проходит: два неотсортированных import-блока (`detection_pipeline.py`, `telegram_bot.py`). `compileall` проходит, `runtime_check.py` проходит для `MARINE_TRACK_PROVIDER_PROFILE=core` с тестовым Telegram token. До исправления этих несоответствий release gate нельзя считать закрытым.

Последний GitHub Actions run остановился на lint, поэтому test job был пропущен. CI запускается только на Python 3.11, хотя package объявляет Python `>=3.10`; нет dependency constraints/lockfile и type-check job. Отдельный `mypy --no-incremental src runtime_check.py` даёт 74 ошибки в 24 файлах: часть относится к отсутствующим optional stubs, но есть и реальные несовпадения типов в Telegram, scene materializer/search и отчёте. В `pyproject.toml` указана MIT license, но файла `LICENSE` в репозитории нет.

### P0-2. CDSE требует нового asset/auth-контракта, а не только нового URL

`src/marine_track/data_sources/stac_provider.py` жёстко задаёт:

```text
https://catalogue.dataspace.copernicus.eu/stac
collections: SENTINEL-1 / SENTINEL-2
```

Актуальный CDSE STAC API использует:

```text
https://stac.dataspace.copernicus.eu/v1/
sentinel-1-grd
sentinel-2-l2a
```

Старый endpoint официально deprecated с 17 ноября 2025 года. Но простой заменой URL проблема не закрывается: актуальные S1 GRD items содержат amplitude COG assets и отдельные calibration/noise XML, auth references и альтернативные способы доступа. Текущая модель `assets: dict[str, str]` теряет media type, roles, polarization/band, scale/offset, nodata, auth и alternate href; materializer умеет подписывать только Planetary Computer URL и не умеет CDSE bearer/S3 flow.

Следовательно, CDSE нельзя считать рабочим fallback без typed `SceneAsset`, auth-aware materializer, range-read canary и offline contract tests. OData должен оставаться резервным каналом поиска/скачивания, потому что CDSE описывает STAC как дополнительный каталог с ограниченным набором коллекций.

### P0-3. Нет научного benchmark и калибровки

`confidence` рассчитывается как:

```text
0.50 * peak_score
+ 0.35 * clamp(contrast_sigma / 8)
+ 0.15 * clamp((elongation - 1) / 5)
```

Это полезный ranking score, но не вероятность. Нет размеченной выборки, negative-only сцен, false-alarm rate на площадь, stratified split и доверительных интервалов. Значения `3.5`, `31`, `5`, `min_area_px=2` сейчас являются эвристическими параметрами, а не результатом оптимизации на независимой выборке.

### P0-4. Wake wavelength пока не является валидной оценкой скорости

Текущий код строит **один cross-axis профиль возле кандидата**, ищет в нём повторяющиеся яркие пики и подставляет медианное расстояние в deep-water Kelvin formula. Этого недостаточно, чтобы утверждать, что измерена `Lmax` поперечной волны Kelvin wake:

- не доказано, что Hough-линия является wake axis, а не берегом/волновым фронтом/артефактом;
- не выделены Kelvin arms и не проверена принадлежность пиков к одной дисперсионной структуре;
- не учитываются конечная глубина, течение, wind-sea, угол визирования и ориентация пиксельной сетки;
- нет оценки неопределённости и минимальной длины наблюдаемого следа;
- для Sentinel-1 и Sentinel-2 не разделены разные механизмы формирования контраста.

Метод должен оставаться research-only: результат хранить как `speed_proxy_knots`, а не как оперативную скорость, пока не появятся paired AIS/разметка и QC.

### P0-5. Search cache может вернуть не тот временной интервал и capability

`search_cache_key()` включает AOI, sensor, длительность окна и `max_results`, но не абсолютные `start/end`, capability запроса, provider order/config и версию схемы. `/dates` и `/detectbbox` используют общий namespace: archive/preview scene из search-only запроса может попасть в detection cache hit без повторной проверки processability. Два сдвинутых окна одинаковой длительности также коллидируют в пределах TTL.

Это silent correctness bug, а не только проблема производительности. Ключ должен включать canonical UTC interval, purpose/capability, effective provider config и schema version; любой cache hit повторно проходит capability/auth/readability validation. Сцены нужно детерминированно сортировать по acquisition time и tie-breaker, а не считать `scenes[0]` самой свежей без явной сортировки.

### P0-6. Пользовательская семантика сейчас утверждает больше, чем измерено

Runtime всё ещё подписывает результаты как `vessels`, «найденные суда», `Судно #`, `conf` и `speed ... kt`. Kelvin proxy записывается в `detection.speed_knots`, а AIS SOG может перезаписать то же поле. Это противоречит научной границе, описанной в документации.

До benchmark UI и schema должны различать `candidate`, `ranking_score`, `research_proxies.kelvin_speed_proxy_knots`, собственную `speed.value_knots=null` и `reference.ais.sog_knots`. Исправление labels/schema — P0, а не косметика.

### P0-7. Telegram access fail-open и scene tokens не изолированы по пользователям

Если `TELEGRAM_ADMIN_IDS` пуст, `is_authorized()` разрешает команды всем; `.env.example` оставляет список пустым. Scene-registry token не связан с user/chat, поэтому знающий token может повторно запустить чужую сцену/AOI. Нет per-user AOI/bytes/time quotas, а registry/user-state пишутся без lock и atomic replace.

Operational default должен быть fail-closed. Публичный режим допускается только через явный флаг, scene token/outputs привязываются к владельцу, state writes становятся atomic/locked, а запросы получают лимиты площади, сложности геометрии, времени, байтов и частоты.

## 5. Научные и алгоритмические слабые места

### 5.1. CFAR не является полноценным guard-cell CFAR

В `cfar_mask()` локальные `mean/std` вычисляются окном, которое включает саму цель. `guard_window_px` применяется после thresholding как `maximum_filter`, а не для исключения guard ring из оценки clutter. На ярких целях фон загрязняется целью, а на краях и рядом с NaN статистика меняет эффективное окно. Следующий detector должен явно разделять:

```text
training ring → guard cells → CUT (cell under test) → robust clutter model → threshold
```

Нужны как минимум valid-count, edge flag, robust quantiles/MAD либо распределение sea clutter и контроль false alarms per km².

### 5.2. Preprocessing sensor-specific не подключён

`config/processing.yaml` декларирует Lee filter, Sentinel-2 bands, SCL/cloud/water mask и tile parameters, но pipeline их не применяет. Сейчас:

- Sentinel-1 читает первый band выбранного raster asset и нормализует его глобальными percentiles;
- единицы продукта (amplitude/DN, sigma0, gamma0, dB) не фиксируются в provenance;
- Sentinel-2 также выбирает один band и пропускает cloud/water mask;
- tile/overlap из конфигурации не используются.

Пока это не исправлено, нельзя смешивать S1/S2 в одной шкале confidence или сравнивать результаты между провайдерами.

### 5.3. Wake association не защищён от ложных линейных объектов

Hough выполняется по исходному crop без land/shoreline mask, без suppression image border и без проверки протяжённости от корпуса в кормовом секторе. Линия принимается, если проходит в пределах 24 px от centroid. В результате берег, край AOI, радиальные волновые фронты и текстура моря могут получить `heading_method=wake_axis`.

Нужны: маска суши в том же crop, water-only valid mask, сектор за корпусом, минимальная длина/непрерывность, bilateral/arm symmetry, локальный contrast вдоль линии и сравнение с альтернативными line hypotheses.

### 5.4. AIS используется как сопоставление, но ещё не как строгая валидация

Текущее сопоставление интерполирует track point, но не ограничивает максимальный разрыв между соседними AIS сообщениями и не учитывает неопределённость времени/геометрии снимка. В pipeline нет полноценного `matched / unmatched / ambiguous / stale / out_of_window` QC. `validation.py` не вызывается, а `validation_status` остаётся `unvalidated`, кроме простого `ais_matched`.

AIS SOG/COG нужно хранить как reference с provenance, а не использовать для незаметной подмены собственного результата. Отдельно хранить расстояние, временной gap, heading difference и число track points.

### 5.5. Провайдеры смешивают search-only и detection-capable контракты

- ASF возвращает полезные Sentinel-1 metadata/preview и ZIP/GRD, но текущий materializer их не обрабатывает.
- Planetary Computer Sentinel-1 RTC практичен для COG: dataset page описывает account/SAS flow, а SDK допускает anonymous use с более строгими rate limits. Текущий абсолютный тезис «credentials не требуются» нужно заменить live sign/range-read preflight конкретного asset.
- CDSE STAC может отдавать COG bands, но требует нового endpoint/collection mapping и авторизации для защищённых assets.
- Sentinel Hub Catalog требует OAuth и не гарантирует, что Catalog item содержит прямой processable COG.

В интерфейсе нужно разделить `search_capable`, `preview_capable`, `raster_capable`, `archive_capable` и не считать сцену обнаруживаемой только по наличию metadata.

### 5.6. Геометрия AOI и память

Некоторые provider adapters берут только первый feature из FeatureCollection. Нет явного контракта для MultiPolygon/antimeridian. Большие raster assets читаются целиком; при отсутствии AOI crop это создаёт риск памяти. Raster cache не имеет lock-файлов, хотя отсутствие lock уже отмечено в плане.

Land-mask installer один раз обрезает маску по `DEFAULT_AOI_GEOJSON`: произвольный `/detectbbox` вне этого района остаётся без подавления суши. Буфер береговой линии вычисляется приближённым переводом метров в градусы, что искажает расстояние на высоких широтах. При `rasterio.mask(..., filled=True)` и неизвестном source nodata вне-AOI нули могут остаться «валидной водой». Нужны versioned global coastline, AOI tile/subset cache, metric/geodesic buffer, explicit valid mask и лимит AOI до чтения/скачивания.

### 5.7. Detector score насыщается и не переносится между сценами

Percentile 2/98 normalization клипирует верхний хвост в `1.0`, поэтому `peak_score` часто насыщается и занимает 50% текущего `confidence`. Reward за elongation может повышать score берегового/волнового clutter. Min/max area заданы в пикселях, а не в м², а полное чтение raster в память исключает контролируемую работу на больших AOI.

Нужны radiometric domain contract, robust training ring, physical-size filters, tiled inference, parameter-persistence feature и отдельные scene-quality/applicability gates. Один score нельзя сравнивать между продуктами, сенсорами и AOI до калибровки.

### 5.8. AIS matching допускает множественное подтверждение и слабый time contract

Каждый candidate независимо выбирает ближайший AIS target, поэтому один MMSI может подтвердить несколько объектов. Нет max interpolation gap, one-to-one assignment, antimeridian-safe interpolation, acquisition start/end/per-pixel time uncertainty и явного margin между первым/вторым match. Wake heading не проверяется относительно качественного AIS reference, если уже заполнен.

AIS неполон и может содержать ошибки/спуфинг, поэтому используется как stratified reference с provenance, а не как безусловная ground truth. Для Sentinel-2 дополнительно важны реальные inter-band acquisition offsets.

### 5.9. Конфигурация, provenance и секреты не образуют воспроизводимый контракт

`config/processing.yaml` загружается, но CLI/Telegram используют hard-coded detector parameters. Фильтры и priority из `config/sources.yaml` применяются непоследовательно. Asset выбирается в том числе по suffix `.tif/.tiff`, что ломается на query URL и игнорирует media type.

`report.json` не фиксирует code commit, effective config, package versions, collection, asset type/auth mode, band/units, transform/GSD и AOI hash. Зато в отчёт могут попасть абсолютные server paths, AIS path и provider error с подписанным URL. Нужны reproducibility manifest и централизованная redaction SAS/query tokens, bearer values, credentials и local paths.

### 5.10. Эксплуатационный state и deploy неатомарны

Cleanup не покрывает фактические `dates_*`, `bboxdates_*`, `detectbbox_*` run directories, registry и previews; state растёт без полного retention contract. Deploy сначала синхронизирует live tree и ставит editable package, затем валидирует; atomic release directory/rollback отсутствуют. Service user имеет write-доступ к install tree, поэтому процесс может модифицировать код/venv.

Целевой deploy: versioned root-owned release, read-only code/venv, отдельные writable state/cache/output dirs, pre-switch checks, atomic symlink switch и rollback.

## 6. Синтез признаков для будущей детекции

Признаки следует хранить раздельно по группам и не сводить сразу в произвольную одну confidence.

### 6.1. Vessel evidence

- local CNR/contrast, robust background scale, peak-to-clutter ratio;
- area, equivalent diameter, length/width, elongation, compactness, solidity, rectangularity;
- distance to coast/land mask, distance to AOI edge, valid-water fraction;
- incidence angle, orbit direction, instrument mode, polarization;
- dual-pol features `VV`, `VH`, `VV/VH` и cross-pol contrast, когда доступны;
- detector stability при изменении window/threshold и повторной обработке с overlap;
- saturation/nodata/edge flags.

### 6.2. Wake evidence

- normalized Hough/Radon accumulator per line length, continuity and gap fraction;
- distance from vessel candidate to wake onset and wake length;
- along-axis and cross-axis contrast, width growth and texture anisotropy;
- number of arms, arm-angle symmetry, vertex proximity, Kelvin-angle residual;
- positive and negative wake components, not only bright ridges;
- spectral/autocorrelation peak stability along the arm, not one profile at the vessel;
- agreement of wake axis with hull orientation when hull is resolvable.

### 6.3. Environment and physics

- wind speed/direction and sea-state proxy;
- current vector and wave height/period from Copernicus Marine or a documented fallback;
- bathymetry/depth and finite-depth flag;
- sensor incidence/azimuth and pixel anisotropy;
- daylight/cloud/glint/SCL flags for optical scenes.

Эти параметры дают не только новый score, но и объяснимый rejection: «кандидат есть, но условия наблюдения не позволяют подтверждать wake/speed».

### 6.4. Новая схема качества

Предлагается хранить четыре независимых результата:

```text
ship_score       — сила признаков корпуса/объекта
wake_score       — сила признаков следа
quality_score    — пригодность сцены и геометрии
uncertainty      — ошибка координат/курса/скорости и quality flags
```

До калибровки на разметке слово `confidence` в пользовательском интерфейсе заменить на `evidence_score` или `ranking_score`. `vessel_confirmed` выставлять только по порогу, зафиксированному на validation split.

Формулы, units/domain, QC и порядок внедрения зафиксированы в отдельном [`FEATURE_CATALOG.md`](FEATURE_CATALOG.md). Ключевые новые производные параметры: robust CNR по training ring, multi-parameter detection persistence, физический resolvedness, normalized wake line strength/continuity, origin residual, aft-sector ratio, 180°-aware alignment, arm-angle residual, multi-profile spectral coherence, finite-depth `k*h` applicability и vector current correction. Они не объединяются в вероятность до calibration split.

## 7. Доступность данных и рекомендуемая иерархия

| Уровень | Источник | Реалистичное назначение |
|---|---|---|
| Primary discovery/materialization target | CDSE STAC v1 | S1 GRD/S2 L2A metadata и COG assets; нужны typed assets, calibration/noise sidecars и auth-aware reader |
| Processing fallback | Planetary Computer | S1 RTC/S1 GRD COG, S2 L2A; catalog и asset access проверять раздельным live preflight |
| Conditional processing fallback | Earth Search | S1 GRD/S2 COG catalog публичен, но S1 `s3://` requester-pays может требовать AWS credentials и создать cost |
| Search/download fallback | CDSE OData | Поиск/загрузка полного продукта, когда STAC не видит сцену или asset |
| Search-only until processor | ASF | Metadata, preview, ZIP/GRD; включать в detection только после SAFE/GRD materializer |
| Optional commercial/API | Sentinel Hub | OAuth Catalog/Process; не считать бесплатным без отдельной проверки quota/contract |
| Ocean context | Copernicus Marine | Global PHY `001_024`, WAV `001_027` и подходящий regional product; точные dataset IDs/variables/version фиксировать в config |
| Bathymetry | GEBCO 2026 | Global 15 arc-second grid; AOI subset и version входят в provenance |
| Reference | Local AIS / NOAA MarineCadastre | Validation; NOAA — исторический американский охват, не глобальный realtime |
| Delayed context/reference | Global Fishing Watch | Hourly public vessel presence с задержкой порядка нескольких суток; не строгая realtime ground truth |

Запрещается выдавать preview за full-resolution asset и считать наличие STAC item гарантией доступности raster. Каждый запуск должен сохранять `asset_key`, media type, href scheme, CRS, GSD, band, units, processing level, auth mode и фактический download status.

## 8. Обновлённое ТЗ и границы MVP

### MVP-0.2: воспроизводимый single-raster candidate detector

Обязательный результат:

1. AOI и UTC interval обрабатываются с единым provider contract.
2. Выбирается только реально читаемый GeoTIFF/COG с описанным band/unit/CRS/GSD.
3. Выполняется sensor-specific preprocessing и water/land validity mask.
4. Результат называется `vessel_candidate`, пока нет benchmark confirmation.
5. Для кандидата сохраняются координаты, uncertainty proxy, evidence features и quality flags.
6. Wake axis выдаётся только при прохождении геометрического QC; иначе `heading_method=not_estimated`.
7. `speed_method=not_estimated` по умолчанию. AIS SOG — внешний reference. Kelvin wavelength — только research field с экспериментальным флагом и неопределённостью.
8. Обязательные артефакты: GeoJSON, CSV/Parquet, overview/crops, `report.json`. HTML переносится в отдельную задачу.
9. Результат воспроизводим по `product_id + asset_key + processing_config + code_commit`.

### MVP-0.3: научно валидированный S1 detector

Добавляется:

- правильный guard-cell CFAR/robust sea-clutter model;
- S1 calibration/unit contract и dual-pol features;
- coastline/edge/water masks;
- wake geometry с arm/continuity/angle QC;
- AIS matching quality and physical validation;
- curated benchmark и stratified evaluation;
- calibrated probability only after calibration split.

### Research-0.4

- Kelvin wavelength/spectral estimator с along-arm sampling, finite-depth/current correction и uncertainty;
- Sentinel-2 multi-band optical detector и validated inter-band timing/geometric model;
- ASF SAFE/GRD processing;
- multi-scene temporal association and track building;
- optional ML model only after a classical baseline and dataset card.

## 9. Критерии приёмки

### Engineering gate

- `pytest`, `ruff`, `bash -n`, clean install и core runtime check проходят;
- provider contract tests offline проверяют endpoint, collection, asset selection и понятный fallback;
- повторный запуск не создаёт повреждённых cache files и не скачивает один asset конкурентно дважды;
- все текущие output artifacts читаются и имеют provenance;
- сервис на ошибке доступа не выдаёт ложный «0 судов».

### Scientific gate

Разделить выборку по sensor/polarization/incidence/wind/depth/coast/open-sea/day-night/cloud и не смешивать соседние кадры одного прохода между train и test.

Метрики:

- detection: precision, recall, F1, POD, FAR, CSI, false alarms/km², median/p95 localization error;
- wake: wake detection rate, false-wake rate, angular MAE/p95 и arm/continuity QC;
- speed: bias, MAE, RMSE, median absolute error, coverage and uncertainty calibration against AIS/independent reference;
- operational: latency, bytes downloaded per request, cache-hit rate and provider success rate.

Порог научной приемки задаётся после pilot benchmark: новая версия должна превосходить простую CFAR baseline на независимом test split с bootstrap confidence intervals. Нельзя объявлять фиксированные проценты качества до сбора representative sample.

## 10. Приоритетный план после аудита

1. **P0 — исправить correctness/security.** Полный cache key + capability revalidation, deterministic newest-scene selection, fail-closed Telegram auth, user-scoped tokens, AOI/quota limits и secret redaction.
2. **P0 — зафиксировать честную семантику output.** Ввести candidate/evidence/quality/uncertainty; разделить operational `speed=null`, Kelvin proxy и AIS reference; заменить пользовательские `vessels/conf` labels.
3. **P0 — восстановить data access через typed asset contract.** CDSE STAC v1/collections, asset metadata/auth/alternates, range-read canary, OData/PC fallback и offline/live provider tests.
4. **P0 — вернуть engineering gate в зелёное состояние.** Stale tests, ruff, mypy baseline, Python support matrix, dependency constraints и явная license.
5. **P1 — подключить processing config и provenance.** Одинаково применять YAML/env к CLI/Telegram; писать sanitized effective config/code/runtime/asset manifest.
6. **P1 — исправить mask/state/deploy.** Global versioned coastline/AOI subsets, metric buffers, atomic locked state/cache и root-owned atomic releases.
7. **P1 — переработать S1 preprocessing/CFAR.** Calibration/unit, valid mask, guard ring, robust clutter, tiled inference и overlap deduplication.
8. **P1 — переработать wake association.** Mask, background suppression, arm/sector/continuity/spectral tests, orientation and applicability QC.
9. **P1 — встроить физическую/AIS validation и benchmark.** One-to-one matching, gap/time uncertainty, current/wave/depth context, negative strata, fixed split и evaluation CLI.
10. **P2 — Sentinel-2 full stack, ASF processing и temporal tracking.** Только после стабильного и измеренного S1 COG baseline.

До выполнения пунктов 1–3 новые providers, ML-модели и «улучшение» wake-speed не должны считаться приоритетом.

## 11. Научная база и актуальные ссылки

- [CDSE STAC documentation](https://documentation.dataspace.copernicus.eu/APIs/STAC.html) и [актуальный STAC endpoint](https://stac.dataspace.copernicus.eu/v1/).
- [CDSE Sentinel-1 GRD collection](https://stac.dataspace.copernicus.eu/v1/collections/sentinel-1-grd), [Sentinel-2 L2A collection](https://stac.dataspace.copernicus.eu/v1/collections/sentinel-2-l2a), [OData documentation](https://documentation.dataspace.copernicus.eu/APIs/OData.html).
- [Copernicus Sentinel-1 mission/data products](https://dataspace.copernicus.eu/data-collections/copernicus-sentinel-missions/sentinel-1).
- [ESA example of Radon-based SAR wake detection](https://earth.esa.int/eogateway/success-story/sar-synergy-data-for-maritime-surveillance/ship-wake-detection-using-sar).
- Mazzeo et al., [A Systematic Review of Ship Wake Detection Methods in SAR Images](https://www.mdpi.com/2072-4292/16/20/3775), 2024.
- Karakuş, Rizaev, Achim, [Ship Wake Detection in SAR Images via Sparse Regularization](https://doi.org/10.1109/TGRS.2019.2947360), 2020.
- [The speed and beam of a ship from its wake's SAR images](https://cris.tau.ac.il/en/publications/the-speed-and-beam-of-a-ship-from-its-wakes-sar-images) — speed requires analysis of wake geometry/spectrum, not a single arbitrary profile.
- Binet et al., [Accurate Sentinel-2 Inter-Band Time Delays](https://isprs-annals.copernicus.org/articles/V-1-2022/57/2022/).
- [Planetary Computer Sentinel-1 RTC dataset](https://planetarycomputer.microsoft.com/dataset/sentinel-1-rtc), [Planetary Computer SDK/auth contract](https://github.com/microsoft/planetary-computer-sdk-for-python), [Earth Search](https://github.com/Element84/earth-search).
- Copernicus Marine: [Global physics forecast `001_024`](https://data.marine.copernicus.eu/product/GLOBAL_ANALYSISFORECAST_PHY_001_024/description), [global waves `001_027`](https://data.marine.copernicus.eu/product/GLOBAL_ANALYSISFORECAST_WAV_001_027/description), [Black Sea physics `007_001`](https://data.marine.copernicus.eu/product/BLKSEA_ANALYSISFORECAST_PHY_007_001/description).
- [GEBCO 2026 grid](https://www.gebco.net/data-products/gridded-bathymetry-data/gebco2026-grid), [NOAA MarineCadastre AIS](https://hub.marinecadastre.gov/pages/vesseltraffic), [Global Fishing Watch API/data availability](https://globalfishingwatch.org/global-fishing-watch-data-availability/).
